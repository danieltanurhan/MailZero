Implementation Steps:
Phase 1: Replace user authentication with Firebase while keeping existing connection system
Phase 2: Add IMAP/SMTP driver alongside existing OAuth drivers
Phase 3: Extend connection creation to handle both OAuth and IMAP/SMTP connections